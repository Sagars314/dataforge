% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/power_sim.R
\name{power_sim}
\alias{power_sim}
\title{Estimate Statistical Power by Simulation}
\usage{
power_sim(
  within = list(),
  between = list(),
  n = 50,
  mu = 0,
  sd = 1,
  r = 0,
  test = c("t_test", "anova", "lm"),
  alpha = 0.05,
  rep = 1000,
  seed = NULL,
  verbose = TRUE
)
}
\arguments{
\item{within}{Named list of within-subject factors passed to
\code{\link{forge_design}}.}

\item{between}{Named list of between-subject factors passed to
\code{\link{forge_design}}.}

\item{n}{Sample size per between-subject cell.}

\item{mu}{Cell means.}

\item{sd}{Cell standard deviations.}

\item{r}{Correlation among within-subject measurements.}

\item{test}{Statistical test to apply:
\itemize{
\item \code{"t_test"} independent samples t-test
\item \code{"anova"} factorial ANOVA
\item \code{"lm"} linear model
}}

\item{alpha}{Significance level. Default = 0.05.}

\item{rep}{Number of simulated datasets.}

\item{seed}{Optional random seed.}

\item{verbose}{Print progress messages.}
}
\value{
A list with elements:
\itemize{
\item power Estimated statistical power
\item replications Number of simulations
\item significant Number of significant results
}
}
\description{
Simulate datasets using \code{\link{forge_design}} and estimate statistical
power as the proportion of simulated analyses with p-values below \code{alpha}.
}
\examples{

# Independent t-test (two groups)
power_sim(
  between = list(group = c("control","treat")),
  n = 40,
  mu = c(0, 0.5),
  sd = 1,
  rep = 500,
  test = "t_test"
)

# One-way ANOVA (3 groups)
power_sim(
  between = list(group = c("A","B","C")),
  n = 30,
  mu = c(0, 0.3, 0.8),
  sd = 1,
  rep = 500,
  test = "anova"
)

# Mixed design ANOVA (within + between)
power_sim(
  within  = list(time = c("pre","post")),
  between = list(group = c("control","treat")),
  n = 40,
  mu = c(10,10,10,13),
  sd = 2,
  r = 0.6,
  rep = 500,
  test = "anova"
)

# Linear model with factorial predictors
power_sim(
  between = list(group = c("A","B")),
  within  = list(time = c("t1","t2","t3")),
  n = 30,
  mu = c(0,0.2,0.5,0,0.4,0.9),
  sd = 1,
  r = 0.5,
  rep = 500,
  test = "lm"
)

}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reshape.R
\name{wide2long}
\alias{wide2long}
\title{Convert Wide Data to Long Format}
\usage{
wide2long(
  data,
  within = NULL,
  dv = "y",
  id = "id",
  sep = "_",
  other_cols = NULL
)
}
\arguments{
\item{data}{A wide data frame.}

\item{within}{Character vector of within-factor names. These correspond to
parts of the wide column names separated by \code{sep}.}

\item{dv}{Character. Name for the new dependent variable column. Default \code{"y"}.}

\item{id}{Character. Name of the subject ID column. Default \code{"id"}.}

\item{sep}{Character. Separator used in wide column names. Default \code{"_"}.}

\item{other_cols}{Character vector of additional columns to carry forward.
If \code{NULL} (default), all factor/character columns are treated as fixed.}
}
\value{
A long data frame with one row per subject × measurement combination.
}
\description{
Reshape a wide data frame (as produced by \code{\link{forge_design}}) to long format,
where within-subject measurements become rows. The column names are parsed
to reconstruct factor columns for each within-subject factor.
}
\examples{
wide <- forge_design(
  within = list(time = c("pre", "post")),
  n = 20, mu = c(5, 8), sd = 1, r = 0.4,
  plot = FALSE
)
long <- wide2long(wide, within = "time")
head(long)

}

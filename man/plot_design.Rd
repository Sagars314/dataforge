% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_design.R
\name{plot_design}
\alias{plot_design}
\title{Plot a Simulated Design}
\usage{
plot_design(
  data,
  x = NULL,
  color = NULL,
  facet = NULL,
  dv = "y",
  geom = c("box", "point"),
  alpha = 0.5,
  palette = NULL,
  title = NULL,
  reverse_x = FALSE
)
}
\arguments{
\item{data}{A data frame (ideally a \code{forge_data} object from
\code{\link{forge_design}}, or a long data frame).}

\item{x}{Character. Column to map to the x-axis.}

\item{color}{Character. Column to map to colour / fill.}

\item{facet}{Character. Column to use for faceting. Optional.}

\item{dv}{Character. Dependent variable column (for long format data).
Default \code{"y"}.}

\item{geom}{Character vector. One or more of \code{"violin"}, \code{"box"}, \code{"point"},
\code{"line"}. Default \code{c("violin", "box")}.}

\item{alpha}{Numeric. Transparency for geoms. Default 0.5.}

\item{palette}{Character. A \code{ggplot2}-compatible colour palette name, or a
character vector of colours.}

\item{title}{Character. Plot title. Optional.}
}
\value{
A \code{ggplot2} object, or \code{NULL} invisibly if ggplot2 is not installed.
}
\description{
Generate a \code{ggplot2} visualization of a simulated data frame produced by
\code{\link{forge_design}}. Supports violin + box + point overlays, and automatically
maps factorial structure to aesthetics.
}
\examples{
df <- forge_design(
  within  = list(time = c("pre","post")),
  between = list(group = c("control","treat")),
  n = 40, mu = c(10,13,10,17), sd = 2, r = .5, plot = FALSE
)
plot_design(df, x = "time", color = "group")

}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/contrasts.R
\name{add_contrast}
\alias{add_contrast}
\title{Add Contrast Codes to a Data Frame}
\usage{
add_contrast(
  data,
  col,
  contrast = "treatment",
  reference = 1,
  .add_cols = TRUE
)
}
\arguments{
\item{data}{A data frame.}

\item{col}{Character. Name of the factor column to code.}

\item{contrast}{Character. One of \code{"treatment"}, \code{"sum"}, \code{"anova"},
\code{"helmert"}, \code{"poly"}, \code{"difference"}.}

\item{reference}{Integer. Index of the reference level (treatment coding
only). Default 1.}

\item{.add_cols}{Logical. If \code{TRUE} (default), add contrast columns to
\code{data}. If \code{FALSE}, return the contrast matrix only.}
}
\value{
The data frame with contrast columns added, or the contrast matrix
if \code{.add_cols = FALSE}.
}
\description{
Generate and attach contrast codes for a factor column. Supports treatment,
sum, Helmert, polynomial, and difference coding — matching the schemes
used in \code{faux::add_contrast()}.
}
\examples{
df <- data.frame(group = factor(rep(c("A","B","C"), each = 10)))
add_contrast(df, "group", contrast = "helmert")

}

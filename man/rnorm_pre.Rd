% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rnorm_forge.R
\name{rnorm_pre}
\alias{rnorm_pre}
\title{Make a Normal Vector Correlated with Existing Vectors}
\usage{
rnorm_pre(x, r = 0, n = NULL, mu = 0, sd = 1, empirical = FALSE)
}
\arguments{
\item{x}{A numeric vector, or a data frame / matrix whose columns serve as
the existing variables.}

\item{r}{Desired correlation(s) between the new vector and \code{x}. If \code{x} has
multiple columns, supply one value per column.}

\item{n}{Integer. Number of observations. Inferred from \code{x} when omitted.}

\item{mu}{Mean of the new variable. Default 0.}

\item{sd}{SD of the new variable. Default 1.}

\item{empirical}{Logical. Enforce exact correlation in the sample.}
}
\value{
A numeric vector of length \code{n}.
}
\description{
Given one or more existing numeric vectors, generate a new normally
distributed vector with specified correlations to each of the existing
vectors (and a specified mean and SD).
}
\examples{
x <- rnorm(200, mean = 5, sd = 2)
y <- rnorm_pre(x, r = 0.7, mu = 10, sd = 3)
cor(x, y)

}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/options.R
\name{dataforge_options}
\alias{dataforge_options}
\title{Set or Get Global dataforge Options}
\usage{
dataforge_options(
  verbose = NULL,
  plot = NULL,
  long = NULL,
  sep = NULL,
  seed = NULL
)
}
\arguments{
\item{verbose}{Logical. If \code{TRUE} (default), functions print informational
messages describing the design.}

\item{plot}{Logical. If \code{TRUE} (default), \code{forge_design()} automatically
plots the design after simulation.}

\item{long}{Logical. If \code{TRUE}, \code{forge_design()} returns data in long
format. Default is \code{FALSE} (wide format).}

\item{sep}{Character. Separator used when constructing column names from
factor level combinations. Default is \code{"_"}.}

\item{seed}{Integer or \code{NULL}. If set, a random seed is applied at the
start of every simulation call for reproducibility.}
}
\value{
When called with arguments, invisibly returns the previous option
list. When called with no arguments, returns the current option list.
}
\description{
Control package-wide defaults such as verbosity, automatic plotting,
output format, and the separator used in column name construction.
}
\examples{
# Turn off automatic plotting and messages
dataforge_options(verbose = FALSE, plot = FALSE)

# Check current options
dataforge_options()

# Reset to defaults
dataforge_options(verbose = TRUE, plot = TRUE, long = FALSE,
                  seed = NULL, sep = "_")

}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mixed_design.R
\name{add_within}
\alias{add_within}
\title{Add Within-Subject Factor Levels}
\usage{
add_within(data, ..., .by = NULL, .prefix = FALSE)
}
\arguments{
\item{data}{A data frame (typically created by \code{\link{add_random}}).}

\item{...}{Named character vector arguments specifying within factors and
their levels, e.g., \code{time = c("pre", "post")}.}

\item{.by}{Character. Optional grouping column; the factor is added within
each level of \code{.by}.}

\item{.prefix}{Logical. If \code{TRUE}, within factor names are prepended to
level names in the new column. Default \code{FALSE}.}
}
\value{
An expanded data frame.
}
\description{
Expand an existing design data frame so that each row is replicated for
every level of a within-subject factor.
}
\examples{
df <- add_random(subject = 20) |>
  add_within(time = c("pre", "mid", "post"))
nrow(df)  # 60

}

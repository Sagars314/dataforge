% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rnorm_forge.R
\name{rnorm_forge}
\alias{rnorm_forge}
\title{Simulate Multiple Correlated Normal Variables}
\usage{
rnorm_forge(
  n = 100,
  vars = NULL,
  mu = 0,
  sd = 1,
  r = 0,
  var_names = NULL,
  empirical = FALSE,
  as_matrix = FALSE
)
}
\arguments{
\item{n}{Integer. Number of observations.}

\item{vars}{Integer. Number of variables. Inferred from \code{mu}, \code{sd}, or
\code{r} when possible.}

\item{mu}{Numeric vector of means (recycled to length \code{vars}).}

\item{sd}{Numeric vector of standard deviations (recycled to length \code{vars}).}

\item{r}{Correlation specification — a scalar, lower-triangle vector, or
full correlation matrix. See \code{\link{cormat}}.}

\item{var_names}{Character vector of column names. Defaults to
\code{"V1"}, \code{"V2"}, …}

\item{empirical}{Logical. If \code{TRUE}, the generated data will have \emph{exactly}
the specified means, SDs, and correlations (as opposed to expected values).}

\item{as_matrix}{Logical. If \code{TRUE}, returns a matrix instead of a
data frame.}
}
\value{
A data frame (or matrix) with \code{n} rows and \code{vars} columns.
}
\description{
Generate a data frame of correlated normally distributed variables using the
Cholesky decomposition of the correlation matrix.
}
\examples{
# Three correlated variables, all pairwise r = 0.6
df <- rnorm_forge(n = 100, vars = 3, mu = c(10, 20, 30),
                  sd = c(1, 2, 3), r = 0.6)
cor(df)

# Using a lower-triangle vector for correlations
df2 <- rnorm_forge(100, vars = 3,
                   r = c(0.2, 0.5, 0.3),
                   var_names = c("x", "y", "z"))
round(cor(df2), 2)

}

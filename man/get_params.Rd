% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{get_params}
\alias{get_params}
\title{Get Distributional Parameters from a Data Table}
\usage{
get_params(
  data,
  between = NULL,
  within = NULL,
  digits = 3,
  usekurtosis = FALSE
)
}
\arguments{
\item{data}{A data frame.}

\item{between}{Character vector of grouping (between-subject) column names.}

\item{within}{Character vector of within-subject column names to summarise.
Defaults to all numeric columns not in \code{between}.}

\item{digits}{Integer. Number of decimal places for rounding. Default 3.}

\item{usekurtosis}{Logical. Include skewness and kurtosis? Default \code{FALSE}.}
}
\value{
A data frame with one row per group (or one row if no grouping) and
columns: \code{n}, \code{var}, \code{mean}, \code{sd}, plus pairwise correlations.
}
\description{
Compute observed means, standard deviations, and pairwise correlations for
numeric columns, optionally stratified by grouping columns.
}
\examples{
df <- forge_design(
  within  = list(time = c("pre","post")),
  between = list(group = c("A","B")),
  n = 50, mu = c(10,12,10,15), sd = 2, r = 0.6,
  plot = FALSE
)
get_params(df, between = "group")

}

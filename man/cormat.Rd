% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cormat.R
\name{cormat}
\alias{cormat}
\title{Build a Correlation Matrix}
\usage{
cormat(r = 0, n = NULL, var_names = NULL)
}
\arguments{
\item{r}{A scalar (applied to all off-diagonal entries), a vector of the
lower triangle (row-wise), or an existing square matrix.}

\item{n}{Integer. Number of variables. Required when \code{r} is a scalar.}

\item{var_names}{Character vector of variable names. Used as row/column
names of the returned matrix.}
}
\value{
A symmetric \verb{n x n} correlation matrix with ones on the diagonal.
}
\description{
Construct a valid symmetric correlation matrix from a scalar, vector, or
existing matrix. The result is always positive semi-definite.
}
\examples{
# Scalar: all pairs correlated at 0.5
cormat(0.5, n = 4)

# Vector of lower triangle values
cormat(c(0.3, 0.5, 0.4, 0.2, 0.6, 0.1), n = 4)

# With variable names
cormat(0.4, n = 3, var_names = c("A", "B", "C"))

}

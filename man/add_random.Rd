% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mixed_design.R
\name{add_random}
\alias{add_random}
\title{Add Random Grouping Factors to a Design}
\usage{
add_random(data = NULL, ..., .nested_in = NULL, .sep = "_")
}
\arguments{
\item{data}{A data frame to extend. If \code{NULL} (default), a new design is
started.}

\item{...}{Named integer arguments specifying each random factor and the
number of levels, e.g., \verb{subject = 30, item = 20}.}

\item{.nested_in}{Character. Name of an existing grouping column within
which the new factor is nested. Use \code{NULL} (default) for crossed factors.}

\item{.sep}{Character. Separator for ID construction. Default \code{"_"}.}
}
\value{
A data frame with one row per combination of the new grouping
levels.
}
\description{
Start or extend a data frame representing a mixed effects design by adding
new random grouping factors (e.g., subjects, items, schools). Designed to
be used with the \verb{\%>\%} pipe.
}
\examples{
# 20 subjects, each seeing 10 items (crossed)
design <- add_random(subject = 20, item = 10)
nrow(design)  # 200

# Nested: students within classes
design2 <- add_random(class = 5) |>
  add_random(student = 8, .nested_in = "class")
nrow(design2)  # 40

}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mixed_design.R
\name{add_between}
\alias{add_between}
\title{Add Between-Subject Factor Assignments}
\usage{
add_between(data, ..., .by = NULL, .prob = NULL, .shuffle = TRUE)
}
\arguments{
\item{data}{A data frame.}

\item{...}{Named character vector arguments specifying factors and levels.}

\item{.by}{Character. Column to group by before assigning. E.g., assign
one level per unique value of \code{"subject"}.}

\item{.prob}{Numeric vector. Sampling probabilities for each level
(automatically normalised). Default is equal probability.}

\item{.shuffle}{Logical. If \code{TRUE} (default), shuffle assignments randomly.}
}
\value{
The data frame with new factor column(s) added.
}
\description{
Randomly (or proportionally) assign factor levels to rows of a design data
frame, creating a between-subject variable.
}
\examples{
df <- add_random(subject = 100) |>
  add_between(group = c("control", "treatment"))
table(df$group)  # ~50 each

# Unequal probability (3:1 ratio)
df2 <- add_random(subject = 80) |>
  add_between(condition = c("A", "B"), .prob = c(3, 1))

}
